#placeholder

def even_numbers(n):
    """Return a list of even numbers up to n (exclusive)."""
    return [i for i in range(n) if i % 2 == 0]  



def nuneven_numbers(n):
    """Return a list of odd numbers up to n (exclusive)."""
    return [i for i in range(n) if i % 2 != 0]  
